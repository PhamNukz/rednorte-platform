server:
  port: 8082

spring:
  application:
    name: ms-agenda-medica-java

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5434/agenda_db}
    username: ${DB_USER:rednorte}
    password: ${DB_PASS:rednorte123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.rednorte: DEBUG
